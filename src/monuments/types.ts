export interface MonumentStructure {
  name: string;
  description: string;
  imageUrl: string;
  country: string;
  city: string;
  id: string;
}

export interface MonumentLocation {
  city: string;
  country: string;
}
